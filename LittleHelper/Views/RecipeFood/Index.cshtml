@model IEnumerable<LittleHelper.Models.RecipeFood.RecipeFoodListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RecipeFoodId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RecipeId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FoodId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RecipeFoodId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RecipeId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FoodId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.RecipeFoodId }, new { @class = "btn btn-success" })
            @Html.ActionLink("Details", "Details", new { id = item.RecipeFoodId }, new { @class = "btn btn-info" })
            @Html.ActionLink("Delete", "Delete", new { id = item.RecipeFoodId }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
